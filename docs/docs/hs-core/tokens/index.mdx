---
id: tokens-index
title: Introduction
slug: /core/tokens
sidebar_position: 1
---

# Hashi Tokens

Tokens are the heart and soul of the design system, hence, they are one of the most valuable assets. These are your source of truth and provides a centralized repository of design constants that will be used throughout the design system.

export const TokenCard = ({ color, to, children }) => {
    return (
        <a href={ `/docs/core/tokens/${to}` }>
            <div className={`hs-doc-card bg-${color}`}>
                <h3>{ children }</h3>
            </div>
        </a>
    )
}

## Token Index

<section class="grid">
    <TokenCard to="color-tokens" color="scarlet">Color</TokenCard>
    <TokenCard to="shape-tokens" color="native-blue">Shape</TokenCard>
    <TokenCard to="typography-tokens" color="plox">Typography</TokenCard>
</section>

